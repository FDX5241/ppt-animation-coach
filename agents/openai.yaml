interface:
  display_name: "PPT Animation Coach"
  short_description: "Build editable Morph animations after reviewing visual assets"
  default_prompt: "Use $ppt-animation-coach to identify the required assets, show me a preview for approval, and then build the selected PowerPoint animation."

policy:
  allow_implicit_invocation: true
